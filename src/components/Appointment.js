import React from 'react'

const Appointment = ({customer}) => {
  // console.log('props', props)
  return (
    <div>
      {customer.firstName}
      {/* Bineta */}
    </div>
  )
}

export default Appointment